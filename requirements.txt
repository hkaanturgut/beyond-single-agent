# Core runtime
azure-ai-projects>=1.0.0b11
azure-ai-inference>=1.0.0b9
azure-identity>=1.18.0
python-dotenv>=1.0.1
pydantic>=2.8.0
PyYAML>=6.0.2
openai>=1.45.0

# Dev / Test
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Note: 'agent-framework' (WorkflowBuilder / ConcurrentBuilder patterns) is
# implemented locally in src/trip_planner/workflow/builder.py using asyncio
# primitives.  Replace with the real package when it becomes available on PyPI.
